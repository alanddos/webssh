{
  "name": "face-api",
  "version": "0.0.0",
  "scripts": {
  "start": "node server.js",
  "build": "ng build",
  "heroku-postbuild": "ng build --prod" // this will be run on Heroku
},
  "private": true,
  "dependencies": {
   "web-terminal":"0.7.0"
  },
  "engines": {
    "node":"8.9.4",
    "npm": "5.6.0"
  }
}
